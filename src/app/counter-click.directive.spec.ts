import { CounterClickDirective } from './counter-click.directive';



describe('CounterClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CounterClickDirective();
    expect(directive).toBeTruthy();
  });
});
